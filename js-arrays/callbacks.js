const nomes = ['cristian', 'richard', 'pietro', 'gabriel'];

/*
nomes.forEach(function(nome) {
    console.log(nome);
})

nomes.forEach(nome => console.log(nome));
*/
function imrpimeNome(nome) {
    console.log(nome);
}
nomes.forEach(imrpimeNome);
